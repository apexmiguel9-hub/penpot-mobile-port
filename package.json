{
  "name": "penpot-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Android shell for Penpot with touch gestures (pinch-zoom, two-finger pan, touch slop)",
  "scripts": {
    "sync": "cap sync android",
    "add:android": "cap add android",
    "open:android": "npx cap open android",
    "build:apk": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}